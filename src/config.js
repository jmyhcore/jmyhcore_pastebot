config = {
    pasta : {
        trigger: ['!паста', '!pasta'],
        cooldown: 30000
    },
    archive: {
        trigger: ['!archive'],
        cooldown: 0
    }
}

module.exports = config